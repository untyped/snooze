(module info (lib "infotab.ss" "setup")
  (define name "spgsql")
  (define compile-omit-files '("samples/sample1.ss"))
  (define blurb
    '("The spgsql library provides classes, datatypes, and procedures "
      "useful for connecting to a PostgreSQL database server over TCP."))
  (define primary-file "spgsql.ss")
  (define homepage "http://schematics.sourceforge.net/spgsql.html")
  (define version "4.0")
  (define doc.txt "doc.txt")
  (define categories '(net)))
